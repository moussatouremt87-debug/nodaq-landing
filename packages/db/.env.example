# Lu automatiquement par le CLI Prisma (migrations). Copie en .env :
#   cp packages/db/.env.example packages/db/.env
# Rôle ADMIN — migrations uniquement. Le runtime utilise APP_DATABASE_URL (voir /.env.example).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/appdb
